numpy>=1.26.0
pymongo>=4.6.0
matplotlib>=3.8.0
python-dotenv>=1.0.0
# sqlite3 já vem na stdlib do Python, não precisa instalar

# Etapas futuras (adicionar conforme avançar):
# pillow>=10.0.0          # manipulação de imagens nos laudos (Etapa 7)
# jinja2>=3.1.0           # templates LaTeX (Etapa 7)
